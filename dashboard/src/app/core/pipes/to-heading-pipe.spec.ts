import { ToHeadingPipe } from './to-heading-pipe';

describe('ToHeadingPipe', () => {
  it('create an instance', () => {
    const pipe = new ToHeadingPipe();
    expect(pipe).toBeTruthy();
  });
});
