import { FormatDatetimePipePipe } from './format-datetime-pipe-pipe';

describe('FormatDatetimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDatetimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
